# Notebook implementation pending

The notebook should use the same environment client and training runner as the command-line and
HF Jobs paths. Introduce data inspection, indexed reset, a scored trajectory, evaluation, and an
optional training run once those implementations exist.
