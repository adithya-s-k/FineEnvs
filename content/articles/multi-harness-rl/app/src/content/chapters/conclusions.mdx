## Conclusions and observations

{/* TODO: conclude from what the evals, training runs and ablations actually showed, not from what
    the architecture promises. Three or four claims, each traceable to a number earlier in the
    article. */}

{/* TODO: the summary observations on multi-harness training. Candidates, to confirm or kill against
    the data:
      - how much of a model's score belongs to the harness rather than the model
      - whether training in one harness transfers to another
      - whether the capture layer's cost is worth what it buys versus per-harness environments
      - what breaks first at scale */}

{/* TODO: what is still open, and what someone else could pick up. Be concrete enough that a reader
    could act on it. */}
